services:
  - type: web
    name: lumqr-lerdo
    env: node
    buildCommand: npm run build
    startCommand: npm start
    plan: starter
    disk:
      name: lumqr-db-disk
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATABASE_PATH
        value: /var/data/lumqr.db

